[project]
name = "youtube-sync"
version = "0.1.0"
description = "Adds my YouTube subscriptions to Feedbin and my liked videos to Notion."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.14.10",
    "ty>=0.0.8",
]
